# frontend-test-treeview-kanban
 A small React + TypeScript + Vite application that implements two reusable components commonly used in frontend interviews:  - A hierarchical **Tree View** with lazy loading and drag‑and‑drop - A **Kanban Board** with draggable cards across columns
